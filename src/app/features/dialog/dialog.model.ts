export interface DialogConfiguration {
  modal?: boolean;
  dialogClass?: string;
}

export interface ImageDialogConfiguration {
  initialTop: number;
  initialLeft: number;
  initialWidth: number;
  initialHeight: number;
  finalTop: number;
  finalLeft: number;
  finalWidth: number;
  finalHeight: number;
}
