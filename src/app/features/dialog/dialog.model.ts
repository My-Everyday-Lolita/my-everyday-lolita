export interface DialogConfiguration {
  modal?: boolean;
  dialogClass?: string;
  data?: any;
}

export interface ImageDialogConfiguration {
  initialTop: number;
  initialLeft: number;
  initialWidth: number;
  initialHeight: number;
  finalTop: number;
  finalLeft: number;
  finalWidth: number;
  finalHeight: number;
}
